import { FormatPokemonData } from './format-pokemon-data';

describe('FormatPokemonData', () => {
  it('should create an instance', () => {
    expect(new FormatPokemonData()).toBeTruthy();
  });
});
